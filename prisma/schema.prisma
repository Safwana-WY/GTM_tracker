// Minimal schema — no models needed since the app uses $queryRaw/$executeRaw
// against the tables created by schema.sql. This file just tells Prisma
// Client how to connect and generate itself.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
